#!/bin/bash

while true
do
  exec=$(/usr/lib/nagios/plugins/check_http -H localhost -u http://localhost)
  exec_result=$?
  if [[ $exec_result -ne 0 ]]; then
        echo "thiss-mdq not working"
  else
     echo "$exec: thiss-mdq working"
     exit 0
  fi
  sleep 1
done